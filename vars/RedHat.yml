---
ssl_certificates_package_list:
  - openssl

ssl_certificates_cnf_path: "{{ ssl_certificates_cnf_path_redhat }}"
ssl_certificates_crt_path: "{{ ssl_certificates_crt_path_redhat }}"
ssl_certificates_key_path: "{{ ssl_certificates_key_path_redhat }}"
