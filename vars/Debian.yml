---
tlscert_package_list:
  - openssl
  # Required for fact script
  - python-magic

tlscert_cnf_path_default: /etc/ssl/cnf
tlscert_crt_path_default: /etc/ssl/certs
tlscert_key_path_default: /etc/ssl/private
